Which registers contain arguments to functions? For example, which register holds 13 in main's call to printf?
pointer to "%d %d\n" in a0
f(8) + 1 = 12 in a1
13 in a2

Where is the call to function f in the assembly code for main?
Where is the call to g? (Hint: the compiler may inline functions.)
They both are never called, the compiler optimized it by adding 4 to f(8).

At what address is the function printf located?
0x6bc

What value is in the register ra just after the jalr to printf in main?
34

Run the following code.
unsigned int i = 0x00646c72;
printf("H%x Wo%s", 57616, (char *) &i);
He110 World

In the following code, what is going to be printed after 'y='? (note: the answer is not a specific value.) Why does this happen?
printf("x=%d y=%d", 3);
my assumption is that printf is expecting 3 arguments, so it reads from register a2 as well, which values are unkown depending on previous instructions.